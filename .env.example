
# Database Configuration
DATABASE_URL=postgresql://user:password@host:port/dbname

# Authentication
# SESSION_SECRET is critical for application security.
# It MUST be set to a long, random, and unique string in your .env file.
# Do not use the example value in production.
# You can generate a strong secret using a command like: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-strong-and-random-session-secret

# App Configuration
NODE_ENV=development
PORT=5000

# Default Admin User Credentials
# It is strongly recommended to change DEFAULT_ADMIN_PASSWORD in your .env file.
# If DEFAULT_ADMIN_USERNAME or DEFAULT_ADMIN_PASSWORD is not set, the default admin user will not be created.
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=changeme
